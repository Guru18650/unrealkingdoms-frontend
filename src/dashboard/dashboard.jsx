import { red } from "@mui/material/colors";

export default function Dashboard() {

  return (
    <div id="dashboard-page">
      <h1 style={{color: "black"}}>Dashboard</h1>
      <p style={{color: "black"}}>This is temporary page after register and login</p>
      
    </div>
  );
}